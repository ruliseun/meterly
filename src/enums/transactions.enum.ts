export enum StripeEventsEnum {
  SUCCEEDED = "charge.succeeded",
  FAILED = "charge.failed",
  SUBSCRIPTION_SUCCEEDED = "invoice.payment_succeeded",
  SUBSCRIPTION_FAILED = "invoice.payment_failed",
}
